import webpack from 'webpack';
import path from 'path';

const webpackConfig = {
    entry: [
    
    ],
    output: {
    
    },
    resolve: {
         extensions: []
    },
    module: {
        loaders: [
    
        ]
    },
    
    plugins: [  

    ]
}

export default config;